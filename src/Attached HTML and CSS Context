Attached HTML and CSS Context

<canvas width="1400" height="240" aria-label="Visible spectrum animation" style="width: 700px; max-width: 700px; display: block; margin: 2.2rem auto 0.8rem; border-radius: 8px; background: rgb(255, 255, 255); box-shadow: rgba(0, 0, 0, 0.04) 0px 2px 8px; height: 120px;"></canvas>


/* Inline style */
element {
  width: 700px;
  max-width: 700px;
  display: block;
  margin: 2.2rem auto 0.8rem;
  border-radius: 8px;
  background: rgb(255, 255, 255);
  box-shadow: rgba(0, 0, 0, 0.04) 0px 2px 8px;
  height: 120px;
  width: 700px;
  max-width: 700px;
  display: block;
  margin-top: 2.2rem;
  margin-right: auto;
  margin-bottom: 0.8rem;
  margin-left: auto;
  border-top-left-radius: 8px;
  border-top-right-radius: 8px;
  border-bottom-right-radius: 8px;
  border-bottom-left-radius: 8px;
  background-image: initial;
  background-position-x: initial;
  background-position-y: initial;
  background-size: initial;
  background-repeat: initial;
  background-attachment: initial;
  background-origin: initial;
  background-clip: initial;
  background-color: rgb(255, 255, 255);
  box-shadow: rgba(0, 0, 0, 0.04) 0px 2px 8px;
  height: 120px;
}

/* Matched Rule from user-agent */
video, canvas, img {
  overflow-x: clip;
  overflow-y: clip;
  overflow-clip-margin: content-box;
}

/* Inherited from ancestor level 1 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 1 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 1 (regular) */
.physical-spectrum-full {
  width: 100%;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
  display: flex;
  flex-direction: column;
  width: 100%;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
  display: flex;
  flex-direction: column;
}

/* Inherited from ancestor level 1 (regular) */
 {
  align-items: center;
  align-items: center;
}

/* Inherited from ancestor level 2 (user-agent) */
article, aside, footer, header, hgroup, main, nav, search, section {
  display: block;
}

/* Inherited from ancestor level 2 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 2 (regular) */
.intro-grid-section, .credits-section, .main-content-container {
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
  width: 100%;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
  width: 100%;
}

/* Inherited from ancestor level 2 (regular) */
.credits-section {
  width: 100%;
  margin: 2.2rem 0 0;
  padding: 0 clamp(1rem,4vw,2.5rem);
  width: 100%;
  margin-top: 2.2rem;
  margin-right: 0px;
  margin-bottom: 0px;
  margin-left: 0px;
  padding-top: 0px;
  padding-right: clamp(1rem, 4vw, 2.5rem);
  padding-bottom: 0px;
  padding-left: clamp(1rem, 4vw, 2.5rem);
}

/* Inherited from ancestor level 3 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 3 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 3 (regular) */
html, body, .App {
  min-height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-stack);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
  min-height: 100%;
  color: var(--text);
  font-family: var(--font-stack);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizelegibility;
}

/* Inherited from ancestor level 3 (regular) */
.App {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 6vh 4vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding-top: 6vh;
  padding-right: 4vw;
  padding-bottom: 6vh;
  padding-left: 4vw;
}

/* Inherited from ancestor level 3 (regular) */
.App {
  display: flex;
  flex-direction: column;
  display: flex;
  flex-direction: column;
}

/* Inherited from ancestor level 4 (user-agent) */
div {
  display: block;
}

/* Inherited from ancestor level 4 (user-agent) */
address, blockquote, center, div, figure, figcaption, footer, form, header, hr, legend, listing, main, p, plaintext, pre, summary, xmp, article, aside, h1, h2, h3, h4, h5, h6, hgroup, nav, section, search, table, caption, colgroup, col, thead, tbody, tfoot, tr, td, th, dir, dd, dl, dt, menu, ol, ul, li, bdi, output, [dir="ltr" i], [dir="rtl" i], [dir="auto" i] {
  unicode-bidi: isolate;
}

/* Inherited from ancestor level 5 (user-agent) */
body {
  display: block;
  margin-top: 8px;
  margin-right: 8px;
  margin-bottom: 8px;
  margin-left: 8px;
}

/* Inherited from ancestor level 5 (regular) */
body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
    'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
    sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  margin-top: 0px;
  margin-right: 0px;
  margin-bottom: 0px;
  margin-left: 0px;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif;
  -webkit-font-smoothing: antialiased;
}

/* Inherited from ancestor level 5 (regular) */
html, body, .App {
  min-height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-stack);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
  min-height: 100%;
  color: var(--text);
  font-family: var(--font-stack);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizelegibility;
}

/* Inherited from ancestor level 5 (regular) */
html, body {
  overflow-x: hidden;
  overflow-x: hidden;
}

/* Inherited from ancestor level 5 (regular) */
html, body {
  -ms-overflow-style: none;
  scrollbar-width: none;
  scrollbar-width: none;
}

/* Inherited from ancestor level 5 (regular) */
body {
  margin: 0;
  margin-top: 0px;
  margin-right: 0px;
  margin-bottom: 0px;
  margin-left: 0px;
}

/* Inherited from ancestor level 6 (user-agent) */
html {
  display: block;
}

/* Inherited from ancestor level 6 (user-agent) */
:root {
  view-transition-name: root;
}

/* Inherited from ancestor level 6 (regular) */
html, body, .App {
  min-height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-stack);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
  min-height: 100%;
  color: var(--text);
  font-family: var(--font-stack);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizelegibility;
}

/* Inherited from ancestor level 6 (regular) */
html, body {
  overflow-x: hidden;
  overflow-x: hidden;
}

/* Inherited from ancestor level 6 (regular) */
html, body {
  -ms-overflow-style: none;
  scrollbar-width: none;
  scrollbar-width: none;
}

/* Inherited from ancestor level 6 (regular) */
:root {
  --bg: #ffffff;
  --text: #111111;
  --text-soft: #444;
  --line: #e5e5e7;
  --radius-s: 6px;
  --radius-m: 14px;
  --shadow-soft: 0 2px 4px rgba(0,0,0,0.04), 0 4px 16px rgba(0,0,0,0.04);
  --font-stack: 'NewYork Web', 'NewYork Local', Georgia, 'Times New Roman', serif;
  --bg: #ffffff;
  --text: #111111;
  --text-soft: #444;
  --line: #e5e5e7;
  --radius-s: 6px;
  --radius-m: 14px;
  --shadow-soft: 0 2px 4px rgba(0,0,0,0.04), 0 4px 16px rgba(0,0,0,0.04);
  --font-stack: 'NewYork Web', 'NewYork Local', Georgia, 'Times New Roman', serif;
}
